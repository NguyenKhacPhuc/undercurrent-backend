[build]
builder = "RAILPACK"

[deploy]
startCommand = "./gradlew run --no-daemon --quiet"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
